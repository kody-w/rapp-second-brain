brain/claims/

# ── Seam: local-only, never publishable ──────────────────────────────────────
# The denylist / guard / private-scope map ENUMERATE the exact private terms that
# must never appear publicly — publishing that list would itself be the leak. They
# live only in the private half (~/SecondBrain/raw/ecosystem). Belt-and-suspenders
# so a stray `git add` can never stage them here. The public-safe *description* of
# the gate stays tracked; only the radioactive artifacts are ignored.
seam-denylist.txt
*.denylist
private-scope-map.md
seam-guard.sh
banned-tokens*
crawl-manifest.json
/raw/
**/raw/
